@page
@model WebApp.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GlobalDataBase[0].TotalConfirmed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GlobalDataBase[0].DateDataBase)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.GlobalDataBase) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TotalConfirmed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateDataBase)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.GlobalDataBaseId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.GlobalDataBaseId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.GlobalDataBaseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
